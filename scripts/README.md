# Seed data

Phase 2 will add SQLAlchemy models and Alembic migrations. Seed scripts will live in `scripts/seed.py`.

Phase 1 has no schema yet — Postgres is provisioned empty for connectivity checks only.